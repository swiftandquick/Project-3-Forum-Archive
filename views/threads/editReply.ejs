<!-- boilerplate.ejs set up the general layout for this ejs file. -->
<% layout('layouts/boilerplate')%>
<!-- The form goes submit a PUT requests on localhost:3000/threads/:id/replies/:replyId, which overrides POST request.  
    The form's inputs have values equal to reply object's current replyContent.  -->
<div class="row">
  <h1 class="text-center">Edit Reply</h1>
  <!--div class="col-6 offset-3">
    <form
      action="/threads/<%=thread._id%>/replies/<%=reply._id%>?_method=PUT"
      method="POST"
      novalidate
      class="validated-form"
    >
        <div class="mb-3">
          <label class="form-label" for="replyContent">Content</label>
          <textarea
            class="form-control"
            rows="5"
            cols="50"
            id="replyContent"
            name="thread[replyContent]"
            required
          >
<%=reply.replyContent%></textarea
          >
          <div class="valid-feedback">Looks good!</div>
        </div>
        <div class="mb-3">
          <button class="btn btn-success">Update Reply</button>
        </div>
      </div>
    </form>
  </div-->
</div>
